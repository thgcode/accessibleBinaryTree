# accessibleBinaryTree
A project I am developing to help me learn binary trees

##Running the code

The code requires Python and WxPython for rendering the tree.

To run it, execute `python tree.py`.
